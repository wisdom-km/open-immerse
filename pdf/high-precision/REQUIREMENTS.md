# 沉浸译 · PDF 论文高精度翻译

需求、架构、技术栈与流程（已确认）

- 日期：2026-09-21；优先级补充：2026-09-22
- 产品：Chrome 扩展「沉浸译 / Open Immerse」（`D:\open-immerse`，MIT）
- 本文状态：**需求已与产品方逐条确认**，作为后续实现的基准。若与对话记录冲突，以本文为准。
- 正本位置：`D:\open-immerse\pdf\high-precision\REQUIREMENTS.md`。执行步骤在同目录的 `EXECUTION.md`，块协议的编码细节在 `ARCHITECTURE.md`。与本文冲突时，先改那两份向本文对齐。

---

## 1. 一句话目标

只忠于用户打开的那份 PDF。第一优先级是内容精准：译文跟抽出的原文一致；公式、图、表跟原文是同一套内容。手段可以用裁图，也可以用别的，只要精准仍在。排版优先级低于精准，同时尽量做到最高。本地 GLM-OCR 只做测试；上线走 OCR API 与大模型 API。

### 1.1 优先级（2026-09-22 确认）

| 序 | 目标 | 做到什么程度 |
| --- | --- | --- |
| 1 | 内容精准 | 硬目标。译文不增、不漏、不改原文的主张。公式、图、架构图、曲线、表的内容与打开的 PDF 一致。`i` 被认成 `n`，或 `n` 被认成 `i`，都算失败 |
| 2 | 排版 | 低于内容。阅读顺序、左右对照、点击对齐、不叠字、字号可读，这些尽量做到最高。任何排版都不得改写上一条 |

公式和图的做法不锁死成唯一技术。默认是从原页裁图，因为这条已经能保住笔画。内嵌图原样取出、原页矢量原样搬过来，也可以，只要右栏结果仍与打开的 PDF 同一套内容。把公式认成 LaTeX 再渲染、把图内英文重画，这两条达不到精准，不采用。拿不准时退回裁图。

---

## 2. 已确认需求

### 2.1 产品边界

| 项 | 确认 |
| --- | --- |
| 载体 | 现有 Chrome 插件，不另做桌面 App |
| 输入 | **只有 PDF**。不依赖 arXiv / LaTeX 源码 |
| 主场景 | 可下载的**发表论文纯 PDF**（数字 PDF，可复制文字） |
| 次场景 | 扫描件**少**，但 **OCR 能力保留**（无文字层时抽正文） |
| 界面 | 左栏 pdf.js 原文；右栏通读对照。点右栏块 → 左栏滚到同一区域并高亮 |
| 开源 | 插件保持 MIT。不把 AGPL 的 BabelDOC 嵌进扩展包 |

### 2.2 必须极准

| 对象 | 标准 | 做法 |
| --- | --- | --- |
| 公式（含行内） | 与原文同一套内容；禁止 i/n 互认 | 默认裁原页。其他手段可以，验收标准相同。不展示 OCR 出来的 LaTeX |
| 图、架构图、曲线图 | 与原文同一张图 | 默认同上。图内英文不重绘、不翻译 |
| 表 | 第一版视觉与原文一致 | 默认整表当图，不译格子。其他手段也要整表内容不变 |
| 正文原文 | 数字 PDF 用文件里的字，不是猜的 | **优先 PDF 文字层原句** |
| 译文 | 跟抽出的原文内容精准 | 只译标题、段落、题注；公式/图/表不进模型。措辞顺滑让路给忠实 |
| 对应关系 | 读者能对上「这段译文 ↔ 原文哪一块」 | 每块 `page + bbox` |

行内公式：夹在句子里的符号也拆出去单独保住（默认是一小段裁图）。字母不送给 OCR，也不送给翻译。

公式在右栏展示原文内容。不做可复制 LaTeX，不用 KaTeX 当公式主展示。第一版的实现手段是原页裁图。

### 2.3 明确不做（第一版）

- 可复制 LaTeX / 把公式认成 TeX 再渲染
- 图内文字译成中文并重画
- 翻译表格单元格
- 生成「长得像原论文」的中文 PDF（BabelDOC 整页回写）
- 多模态「看一整页直接出中文」
- 把译文糊进原 PDF 文字框（bbox 镜像；仓库内 #40 已否决）

### 2.4 准确度目标（能做多高做多高）

没有开源方案能对「任意 PDF + 任意句译文」签 99% 的法律保证。本项目用技术选型把 **可保证的部分推到接近 99%**，把不可保证的部分隔离并写明。

| 项 | 数字发表 PDF（主场景） | 扫描件（保留能力） |
| --- | --- | --- |
| 公式视觉 | 目标 **≥ 99%**（原页裁图，不改笔画） | 与该页扫描件一致（糊/歪属原件） |
| 图 / 架构图 | 目标 **≥ 99%**（原页裁图） | 同上 |
| 表（当图） | 目标 **≥ 99%** | 同上 |
| 正文是否抽全、抽对 | 文字层路径目标 **尽量 99%**；框划错会掉点 | OCR 上限，允许低于数字 PDF，界面提示 |
| 译文相对「抽出的原文」 | 尽量高（强模型 + 术语表 + 只译人话）；**不是公式那种像素级 99%** | 错字会传递到译文 |

内容精准是硬目标，排版是第二目标。公式、图、表用「与打开的 PDF 同一套内容」来验收，不把某一种裁图函数写成唯一合法手段。译文用强 LLM API、术语约束、数字和变量名原样保留来逼近；不在第一版用自动 BLEU 当门槛，也不为了更好看的段落去改主张。排版在不伤害内容的前提下尽量做好：阅读序、分栏不串、点击能对回左栏、右栏不叠字。

### 2.5 部署与 API（必须预留）

| 阶段 | OCR 划区 | 翻译 |
| --- | --- | --- |
| 本地测试 | 本机 GLM-OCR | 插件已配置的大模型 API，或小规模购买 API |
| 小规模试 API | 智谱 GLM-OCR 等云 API（同一 JSON） | 同上（Ark / DeepSeek / OpenAI 兼容） |
| 上线 | **只走 OCR API + LLM API** | 密钥仍在用户/产品配置的 `chrome.storage` |

架构上 **本地引擎与云 API 必须同一套块协议**。换后端只换 adapter，不改右栏渲染和裁图逻辑。  
**公式、图、表的内容永远来自用户打开的 PDF，不来自 OCR API 返回的字母。** 默认在浏览器里裁原页。换一种展示手段时，验收仍是同一套内容。这样上线接 API 也不会再出现 i/n 互认。

---

## 3. 从开源抄什么、不抄什么

| 项目 | 借鉴 | 不采用 |
| --- | --- | --- |
| BabelDOC / pdf2zh | 公式和图当原件搬走；只译人话；段级翻译；术语表 | 整段 AGPL 代码；字体正则当唯一公式检测；把译文塞回原框 |
| GLM-OCR / MinerU / PaddleOCR-VL | **只借划区**：label + bbox | **不借公式 LaTeX 上屏**（已实测会把 n 认成 i） |
| 沉浸译现有 PDF 轨 | 左 pdf.js、右通读、`OI_TRANSLATE_BATCH`、实验室开关 | 文字层「恢复 LaTeX」作为公式主路径 |

核心原则：**分类用模型，展示用原文，翻译只用自然语言。**

---

## 4. 总体架构

Chrome MV3 跑不动 0.9B 视觉模型。拆成三层：

```
┌──────────────────────────────────────────────────────────┐
│  Chrome 扩展（MIT，D:\open-immerse）                       │
│  左：pdf.js 原文                                           │
│  右：通读 = 译文段落 + 公式/图/表的原页裁图                  │
│  翻译：现有 OI_TRANSLATE_BATCH（LLM API）                   │
│  裁图：按 bbox 从当前页 canvas 截取（公式准度的根）          │
└────────────────────┬─────────────────────────────────────┘
                     │ 同一 JSON：blocks[]
                     │ 本地 HTTP 或 云 HTTPS（预留）
┌────────────────────▼─────────────────────────────────────┐
│  划区后端（可切换 adapter）                                 │
│  测试：本机 GLM-OCR sidecar                                │
│  试 API / 上线：GLM-OCR API（或同协议的其他 OCR API）        │
│  输出：每块 label + bbox +（仅 text/title/caption 的原文）    │
└──────────────────────────────────────────────────────────┘
```

插件 **不包含** PyTorch、vLLM、GLM-OCR 权重、BabelDOC。

---

## 5. 技术栈

### 5.1 插件内（已有 + 要加的）

| 层 | 技术 | 职责 |
| --- | --- | --- |
| 运行时 | Chrome MV3，现有 manifest | 增加划区服务 `host_permissions`（本机端口 + 上线 OCR API 域） |
| 左栏 | pdf.js 4.10.38（已 vendor） | 原文、缩放、滚动、高亮 bbox |
| 右栏 | `lib/pdf-readout.js` 通读皮肤 | 展示译文 + **原页裁图**，不以 KaTeX 公式为主 |
| 翻译 | 现有 service worker `OI_TRANSLATE_BATCH` | 只接收纯文本段；可带术语表 system prompt |
| 开关 | `features.pdf`（实验室，默认关） | 再加引擎：`text-layer` / `local-ocr` / `cloud-ocr` |
| 导出 | 现有 `lib/export.js` | 通读 MD：正文译文 + 公式/图用图片或「见图」 |

### 5.2 划区后端

| 层 | 测试 | 上线（预留） |
| --- | --- | --- |
| 模型 | 本机 `zai-org/GLM-OCR` + vLLM / `glmocr[selfhosted]` | 智谱 GLM-OCR HTTP API（输入输出约 0.2 元/百万 tokens） |
| 服务 | 本机 FastAPI `127.0.0.1:<port>` | 同一 adapter 改 base URL + Key |
| 显存 | 约 4–8GB NVIDIA（仅开发机） | 用户无需 GPU |
| 许可 | 权重按上游；**不要**把 AGPL 引擎打进 MIT 扩展仓 | API 条款单独遵守 |

后续可加 MinerU / PaddleOCR-VL adapter，**输出必须仍是第 6 节的 `blocks[]`**。

### 5.3 翻译后端（已有，预留增强）

- OpenAI 兼容 Chat Completions（当前可用 Ark / DeepSeek 等）
- 密钥：`chrome.storage.sync`（用户自备或产品配置）
- 增强（为实现「尽量 99%」）：PDF 专用 prompt、术语表、占位符（数字、变量名）、按段带上下文
- **禁止**把公式字符串、图内 OCR 字送进翻译模型

---

## 6. 块协议（本地与 API 必须一致）

```json
{
  "page": 3,
  "width": 612,
  "height": 792,
  "blocks": [
    {
      "id": "p3-b12",
      "label": "text",
      "bbox": [0.12, 0.40, 0.48, 0.55],
      "text": "We propose the Transformer..."
    },
    {
      "id": "p3-b13",
      "label": "formula",
      "bbox": [0.15, 0.56, 0.85, 0.62]
    },
    {
      "id": "p3-b14",
      "label": "figure",
      "bbox": [0.10, 0.10, 0.90, 0.38],
      "caption": "Figure 1: The Transformer architecture."
    },
    {
      "id": "p3-b15",
      "label": "table",
      "bbox": [0.12, 0.70, 0.88, 0.92]
    }
  ]
}
```

约定：

- `bbox`：归一化 `[x0,y0,x1,y1]`，与 pdf.js viewport 相乘即可裁图和高亮。
- `label`：`title` | `text` | `caption` | `formula` | `figure` | `table` | `header` | `footer`。
- **仅** `title` / `text` / `caption` 带 `text`，且数字 PDF 的 `text` **优先来自文字层与框的对齐**，不是 OCR 猜的公式字母。
- `formula` / `figure` / `table` **禁止**带上屏用的 `latex`。API 若返回 LaTeX，适配器丢弃，不得进入右栏。
- `caption` 可译；图本身只裁图。

---

## 7. 核心流程

```
1. 用户打开 PDF
2. 左栏立刻用 pdf.js 渲染原文（不等划区）
3. 插件把 PDF（或逐页渲染图 + 文字层）交给划区后端
     测试 → 本机 GLM-OCR
     试跑/上线 → OCR API
4. 后端只返回 blocks[]（框 + 正文原文）
5. 插件按 bbox 保住公式/图/表：默认从原页裁图；其他手段须与原页同一套内容
6. 可译块（title/text/caption）→ OI_TRANSLATE_BATCH
     术语表 + 不译数字/占位符
7. 右栏按阅读序：
     译文段落
     公式 = 原页裁图
     图 = 原页裁图 + 译后题注
     表 = 原页裁图
8. 点击右栏块 → 左栏 scrollIntoView + 高亮同一 bbox
9. 无文字层：正文改走 OCR 字，界面提示「本页可能为扫描件，正文或有识别误差」
```

数字论文主路径：**正文 = 文字层 ∩ 正文框**；**公式/图/表 = 原页图**。扫描件只替换「正文从哪来」，裁图逻辑不变。

---

## 8. 准确度如何靠技术栈撑起来

| 风险 | 对策 |
| --- | --- |
| 公式 OCR 把 n 认成 i | 公式不上 OCR 字母；只裁原图 |
| 行内公式被当正文送去翻译 | 行内也标 `formula` 并裁图 |
| 图被重绘译坏 | 图内不译 |
| 表结构译乱 | 整表当图 |
| 正文抽错、抽漏 | 数字 PDF 用文字层；划区框与文字层求交；页眉页脚丢掉 |
| 译文不忠于原文 | 强 LLM API；按段；术语表；只译人话 |
| 上线 API 变差 | 裁图仍在浏览器；API 只划区 |

验收（建议用 Attention is All You Need 及另一份非 arXiv 的出版社 PDF）：

- 右栏公式与左栏同一位置肉眼一致，抽查无 n/i 类改字
- 架构图与原文一致，图内英文未被改
- 表为原图表，未拆格
- 右栏段落能点回左栏对应句
- 关闭本地引擎、改 OCR API 后，公式图仍然来自 PDF 裁图，不回归「认 LaTeX」

---

## 9. 建议落地顺序

1. 冻结本文协议；插件增加 `cloud-ocr` / `local-ocr` 开关与 `blocks[]` 类型
2. 右栏：`formula`/`figure`/`table` 改为 canvas 裁图（不依赖后端是否本机）
3. 本机 sidecar：GLM-OCR → `blocks[]`（测试）
4. 文字层与正文框对齐，作为数字 PDF 的 `text`
5. 翻译侧：术语表 + 过滤非 text 块
6. 同一 adapter 接 GLM-OCR API，小规模付费试跑
7. 无文字层时启用 OCR 正文 + 提示文案
8. （以后可选，不在本文第一版）独立工具导出对照 PDF；表内文字翻译

---

## 10. 仓库与许可

- 扩展：`D:\open-immerse`，MIT。实现时改 `pdf/`、`lib/pdf-readout.js`、`background/`、`manifest.json` 权限。
- 本机 sidecar：建议 **独立目录/独立仓库**，避免把权重和 AGPL 依赖打进扩展发行包。
- 需求正本：`pdf/high-precision/REQUIREMENTS.md`（本文件）。`G:\Notes\PDF高精度翻译\` 只留指针，不再单独改。

---

## 11. 需求确认记录

| 议题 | 结论 |
| --- | --- |
| 表 | 当图 |
| 行内公式 | 拆成图 |
| 扫描件 | 主场景不是扫描；**保留 OCR** 抽无文字层正文 |
| 正文 | 发表纯 PDF → **文字层原句** |
| 公式展示 | 直接贴原图 |
| LaTeX 可复制 | 不做 |
| 忠于原文 | 能做多高做多高；视觉目标按 99% 设计技术栈 |
| 优先级（2026-09-22） | 内容精准第一：译文跟原文，公式/图/表跟原页。手段可裁图也可换，验收不变。排版第二，尽量做到最高 |
| 上线 | OCR API + LLM API；本地仅测试；允许买 API 小规模测 |
| 文档文件名 | `pdf/high-precision/REQUIREMENTS.md` |
